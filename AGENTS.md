# Orchestration Agent Entry

1. Read the operator's machine-wide AGENTS.md and its canonical global wiki first when available; they own machine-wide rules. See README for external document dependencies. Do not copy those rules here.
2. Read `README.md` for scope and local `tasks/paperclip-pilot.md` for the current pilot when available; `docs/local-setup.md` owns the portable setup guide.
3. Keep uncommitted ideas in `.ideas/`. Use one local, Git-ignored `tasks/<work>.md` per work item, including scope, decisions, checklist, and evidence. Do not force-add task records to Git. Add durable verified project facts to `docs/` when they exist.
4. Shared product workflow: the operator-provided `PRODUCT_WORKFLOW_FILE`. Link rather than duplicate it. The repository model is in `skills/squad-model/`.
5. Read `AGENTS.local.md` when present for machine-local knowledge entry points. Keep that file out of Git.
